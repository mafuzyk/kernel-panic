# Handoff — M3 macOS layered reveal mechanic

## Branch and status

- Branch: `codex/plan-execution`
- Worktree: `/tmp/kernel-panic-plan-execution`
- No merge into `main`.

## Delivered

- Selected and documented layered visibility as the Mac act's teachable rule.
- Added stable geometric countdown telegraph and `BACKGROUND` label.
- Paused parent enemy simulation and monitorability during the warning.
- Re-enabled the enemy through a child signal after the authored delay.
- Added `background` state support to the shared vNext renderer contract.
- Applied the rule at the generic Spawner stage boundary for all Mac stages.

## Evidence

```text
/tmp/m3-import2.log
  editor import exit 0

/tmp/m3-green3.log
  exit 0, PROBE_DONE fails=0
```

The focused probe uses a real Spawner story stage and checks both sides of the
lifecycle. It runs with isolated `XDG_DATA_HOME` in normal use.

## Not claimed

- Human balance/readability approval.
- Physical Android/mobile touch behavior or Vega frame budget.
- The final Mac boss/reward route.

## Next task

M4: exercise the complete Mac route, define the climax/boss behavior and make
reward/progression persistence survive save transfer and interrupted/failing
writes. Preserve the layered reveal contract unless direct playtest evidence
shows it is unfair or visually unreadable.
